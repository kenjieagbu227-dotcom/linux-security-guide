# SSH Security Best Practices

- Prefer **key-based authentication** over passwords
- Disable root login if possible
- Change the default port (optional hardening)
- Use strong key passphrases
- Keep OpenSSH updated
- Restrict access with firewall rules or AllowUsers
- Monitor authentication logs for brute-force attempts